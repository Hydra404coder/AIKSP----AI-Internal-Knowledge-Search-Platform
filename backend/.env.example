# =============================================================================
# ENVIRONMENT VARIABLES CONFIGURATION
# =============================================================================
# 
# WHAT IS THIS FILE?
# This file shows all the environment variables your backend needs.
# Copy this file to `.env` and fill in your actual values.
# 
# WHY USE ENVIRONMENT VARIABLES?
# - Security: Secrets never go into your code
# - Flexibility: Different values for dev/staging/production
# - Industry Standard: Every company does this
# 
# HOW IT WORKS:
# 1. Copy this file: cp .env.example .env
# 2. Fill in your values in the .env file
# 3. The dotenv package loads these into process.env
# 4. Your code reads them via process.env.VARIABLE_NAME
# =============================================================================

# -----------------------------------------------------------------------------
# SERVER CONFIGURATION
# -----------------------------------------------------------------------------
# PORT: Which port your Express server listens on
# NODE_ENV: development, staging, or production
PORT=5000
NODE_ENV=development

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION  
# -----------------------------------------------------------------------------
# MONGODB_URI: Connection string to your MongoDB database
# Format: mongodb://username:password@host:port/database_name
# For local: mongodb://localhost:27017/aiksp
# For Docker: mongodb://mongo:27017/aiksp (mongo is the service name)
# For Atlas: mongodb+srv://user:pass@cluster.mongodb.net/aiksp
MONGODB_URI=mongodb://localhost:27017/aiksp

# -----------------------------------------------------------------------------
# JWT (JSON Web Token) CONFIGURATION
# -----------------------------------------------------------------------------
# JWT_SECRET: A long random string to sign your tokens
# IMPORTANT: Make this very long and random in production!
# Generate one at: https://randomkeygen.com/
# JWT_EXPIRES_IN: How long tokens are valid (e.g., 1d, 7d, 30d)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# -----------------------------------------------------------------------------
# GOOGLE GEMINI AI CONFIGURATION
# -----------------------------------------------------------------------------
# GEMINI_API_KEY: Your API key from Google AI Studio
# Get it from: https://makersuite.google.com/app/apikey
# This is used for document summarization and Q&A
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-2.0-flash
GEMINI_MINDMAP_PLAN=false

# -----------------------------------------------------------------------------
# FILE UPLOAD CONFIGURATION
# -----------------------------------------------------------------------------
# MAX_FILE_SIZE: Maximum file size in bytes (10MB = 10485760)
# UPLOAD_PATH: Where uploaded files are stored
MAX_FILE_SIZE=10485760
UPLOAD_PATH=./uploads

# -----------------------------------------------------------------------------
# CORS CONFIGURATION
# -----------------------------------------------------------------------------
# FRONTEND_URL: The URL of your React frontend
# This allows cross-origin requests from your frontend
FRONTEND_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# RATE LIMITING
# -----------------------------------------------------------------------------
# Protects your API from abuse
# Increased from 100 to 1000 to allow normal usage patterns
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=1000
